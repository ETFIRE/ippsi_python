#!/usr/bin/python3

import os


apath = os.path.expanduser("~/a.log")
print("chemin etedu en ", apath)
with open(apath) as fd:
	for line in fd:
		print(line.split(Noneb 11))
		ip, login, passws, date, zone, reqtype
		break
